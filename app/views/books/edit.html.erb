<h1><%= t 'views.common.title.edit', name: Book.model_name.human %></h1>

<%= render "form", book: @book %>

<br>

<nav class="page-nav">
  <%= link_to t('views.common.show', name: Book.model_name.human), @book %>
  <%= link_to t('views.common.back', name: i18n_pluralize(Book.model_name.human)), books_path %>
</nav>
